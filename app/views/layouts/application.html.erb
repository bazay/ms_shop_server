<!doctype html>
<html lang='en'>
  <head>
    <title>MS Online Store</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
  </head>

  <body class="bg-light">
    <div class="container">
      <%= render 'layouts/flash_messages' %>

      <%= yield %>

      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
